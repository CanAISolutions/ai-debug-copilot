import { describe, expect, it } from 'vitest';

// The VS Code APIs are unavailable in Vitest environment; use dependency injection or mocks.
// This skeleton ensures the test suite is discovered and can be expanded.

describe('Extension activation', () => {
  it('should be defined', () => {
    // placeholder assertion
    expect(true).toBe(true);
  });
});